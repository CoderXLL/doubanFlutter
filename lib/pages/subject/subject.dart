import 'package:flutter/material.dart';

class XLLSubjectPage extends StatelessWidget {
  XLLSubjectPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text("书影音"),
        elevation: 0,
      ),
      body: Center(
        child: Text("书影音"),
      ),
    );
  }
}
